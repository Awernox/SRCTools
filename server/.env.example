# Secrets. Add real values in Railway Variables, never in Git.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/WEBHOOK_ID/WEBHOOK_TOKEN
SPEEDRUN_API_KEY=

# Optional configuration.
CHECK_INTERVAL_SECONDS=6
MONITORED_EVENTS=new,verified,rejected

# Default and maximum scope: Run pro + Bhop pro. You may narrow it to one ID.
MONITORED_GAME_IDS=o1yj25r1,268q8o6p
